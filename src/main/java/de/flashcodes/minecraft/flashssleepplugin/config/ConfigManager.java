package de.flashcodes.minecraft.flashssleepplugin.config;

import io.github.flashbtw.util.ConfigUtility;
import org.bukkit.plugin.Plugin;

public class ConfigManager extends ConfigUtility {
    public ConfigManager(Plugin plugin) {
        super(plugin);
    }
}
